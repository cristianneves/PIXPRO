# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
# pre-push:
#   jobs:
#     - name: packages audit
#       tags:
#         - frontend
#         - security
#       run: yarn audit
#
#     - name: gems audit
#       tags:
#         - backend
#         - security
#       run: bundle audit
#
# pre-commit:
#   parallel: true
#   jobs:
#     - run: yarn eslint {staged_files}
#       glob: "*.{js,ts,jsx,tsx}"
#
#     - name: rubocop
#       glob: "*.rb"
#       exclude:
#         - config/application.rb
#         - config/routes.rb
#       run: bundle exec rubocop --force-exclusion {all_files}
#
#     - name: govet
#       files: git ls-files -m
#       glob: "*.go"
#       run: go vet {files}
#
#     - script: "hello.js"
#       runner: node
#
#     - script: "hello.go"
#       runner: go run

pre-commit:
  parallel: true
  # commands:
    # lint-api:
    #   run: cd api && pnpm run lint

    # format:
    #   root: api
    #   glob: "*.ts"
    #   run: pnpm prettier --write {staged_files}

    # check-service-spec:
    #   run: |
    #     bash ./scripts/check-service-spec.sh {staged_files}

    # test-api:
    #   root: api
    #   glob: "*.{spec.ts}"
    #   run: pnpm test

commit-msg:
  commands:
    lint-commit-msg:
      run: npx commitlint --edit {1}
