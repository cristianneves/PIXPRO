stages:
  - test
  - package
  - docker

variables:
  MAVEN_OPTS: "-DskipTests"
  DOCKER_TLS_CERTDIR: "/certs"
  IMAGE_NAME: "$CI_REGISTRY_IMAGE/api-gateway"

cache:
  key: maven
  paths:
    - .m2/repository

api_gateway:test:
  image: maven:3.9.9-eclipse-temurin-17
  stage: test
  allow_failure: true
  rules:
    - changes:
        - pixpro-backend/api-gateway/**
        - pixpro-backend/pom.xml
        - pixpro-backend/api-gateway/pom.xml
    - when: manual
  before_script:
    - cd pixpro-backend
  script:
    - mvn -B -pl api-gateway -am test || echo "⚠️ Test execution failed, marking as warning"

api_gateway:package:
  image: maven:3.9.9-eclipse-temurin-17
  stage: package
  needs: ["api_gateway:test"]
  rules:
    - changes:
        - pixpro-backend/api-gateway/**
        - pixpro-backend/pom.xml
        - pixpro-backend/api-gateway/pom.xml
  before_script:
    - cd pixpro-backend
  script:
    - mvn -B -pl api-gateway -am -DskipTests package

api_gateway:docker:
  image: docker:27.0.3
  stage: docker
  needs: ["api_gateway:package"]
  services:
    - name: docker:27.0.3-dind
      command: ["--tls=false"]
  rules:
    - changes:
        - pixpro-backend/api-gateway/**
        - pixpro-backend/api-gateway/Dockerfile
  variables:
    DOCKER_DRIVER: overlay2
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
  script:
    - cd pixpro-backend
    - docker build -f api-gateway/Dockerfile -t "$IMAGE_NAME:$CI_COMMIT_SHORT_SHA" .
    - docker push "$IMAGE_NAME:$CI_COMMIT_SHORT_SHA"
    - |
      if [ "$CI_COMMIT_BRANCH" = "$CI_DEFAULT_BRANCH" ]; then
        docker tag "$IMAGE_NAME:$CI_COMMIT_SHORT_SHA" "$IMAGE_NAME:latest"
        docker push "$IMAGE_NAME:latest"
      fi
