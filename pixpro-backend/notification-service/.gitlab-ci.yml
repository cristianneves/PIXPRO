stages:
  - test
  - package
  - docker

variables:
  MAVEN_OPTS: "-DskipTests"
  DOCKER_TLS_CERTDIR: "/certs"
  IMAGE_NAME: "$CI_REGISTRY_IMAGE/notification-service"

cache:
  key: maven
  paths:
    - .m2/repository

notification_service:test:
  image: maven:3.9.9-eclipse-temurin-17
  stage: test
  allow_failure: true
  rules:
    - changes:
        - pixpro-backend/notification-service/**
        - pixpro-backend/pom.xml
        - pixpro-backend/notification-service/pom.xml
    - when: manual
  before_script:
    - cd pixpro-backend
  script:
    - mvn -B -pl notification-service -am test || echo "⚠️ Test execution failed, marking as warning"

notification_service:package:
  image: maven:3.9.9-eclipse-temurin-17
  stage: package
  needs: ["notification_service:test"]
  rules:
    - changes:
        - pixpro-backend/notification-service/**
        - pixpro-backend/pom.xml
        - pixpro-backend/notification-service/pom.xml
  before_script:
    - cd pixpro-backend
  script:
    - mvn -B -pl notification-service -am -DskipTests package

notification_service:docker:
  image: docker:27.0.3
  stage: docker
  needs: ["notification_service:package"]
  services:
    - name: docker:27.0.3-dind
      command: ["--tls=false"]
  rules:
    - changes:
        - pixpro-backend/notification-service/**
        - pixpro-backend/notification-service/Dockerfile
  variables:
    DOCKER_DRIVER: overlay2
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
  script:
    - cd pixpro-backend
    - docker build -f notification-service/Dockerfile -t "$IMAGE_NAME:$CI_COMMIT_SHORT_SHA" .
    - docker push "$IMAGE_NAME:$CI_COMMIT_SHORT_SHA"
    - |
      if [ "$CI_COMMIT_BRANCH" = "$CI_DEFAULT_BRANCH" ]; then
        docker tag "$IMAGE_NAME:$CI_COMMIT_SHORT_SHA" "$IMAGE_NAME:latest"
        docker push "$IMAGE_NAME:latest"
      fi
